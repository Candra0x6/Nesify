export type Ticket = {
  eventDescription: string;
  eventId: string;
  eventName: string;
  gbpPrice: string;
  imageUri: string;
  location: string;
  price: string;
  quantity: number;
  startDate: string;
  ticketDescription: string;
  ticketName: string;
  tokenId: number;
};
